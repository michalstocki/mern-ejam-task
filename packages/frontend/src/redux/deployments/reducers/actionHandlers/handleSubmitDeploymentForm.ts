import { SubmitDeploymentFormAction } from '../../actions/submitDeploymentForm';
import { DeploymentsState } from '../deployments';

export function handleSubmitDeploymentForm(
  state: DeploymentsState,
  action: SubmitDeploymentFormAction
): DeploymentsState {
  return state;
}
