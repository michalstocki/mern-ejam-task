version: 2.1
orbs:
  puppeteer: threetreeslight/puppeteer@0.1.1
jobs:
  test:
    docker:
      - image: circleci/node:12
    steps:
      - checkout
      - puppeteer/install
      - restore_cache:
          key: yarn-deps-{{ checksum "yarn.lock" }}
      - run:
          name: "Install dependencies"
          command: yarn
      - save_cache:
          key: yarn-deps-{{ checksum "yarn.lock" }}
          paths:
            - ./node_modules
      - run:
          name: "Run tests"
          working_directory: packages/test
          command: ./node_modules/.bin/jest --maxWorkers=2 --silent --verbose

workflows:
  version: 2
  "build and test":
    jobs:
      - test
